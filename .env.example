# Gemini Developer API key (AI Studio → https://aistudio.google.com/apikey)
# Used server-side only; never shipped to the browser. Without it the app
# falls back to local @imgly background removal.
GEMINI_API_KEY=

# Optional overrides
# GEMINI_IMAGE_MODEL=gemini-2.5-flash-image
# PORT=8787
